# RLVR Experiments Dependencies
# For Lightning AI (A100)

# Core ML
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.25.0
peft>=0.7.0
bitsandbytes>=0.41.0

# Training
trl>=0.27.0  # >=0.27 required for cispo loss_type; >=0.17 for dr_grpo

# Data
reasoning-gym
datasets>=2.14.0
pandas>=2.0.0
pyarrow>=14.0.0

# Utils
pyyaml>=6.0
tqdm
math-verify
tensorboard
